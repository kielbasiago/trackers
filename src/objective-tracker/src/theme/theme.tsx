import { createTheme } from "@mui/material";

export const theme = createTheme({
    typography: {
        allVariants: {
            fontFamily: '"Final Fantasy 3/6 Font"',
            fontSize: "1.35em",
        },
        h6: {
            fontSize: "1.5em",
        },
    },
});

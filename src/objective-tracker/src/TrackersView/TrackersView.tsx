import React from "react";
import styled from "@emotion/styled";

type Props = Record<string, unknown>;

const TrackersView: React.FC<Props> = (props: Props) => {
    const {} = props;
    return <></>;
};

export default TrackersView;
